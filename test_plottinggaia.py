from plottinggaia import plottinggaia
import pytest


#travis tests
#these are independent of the tests already written in the prgram

#idiot test
def test_highsnobjects1()

	assert 1==1


#make sure we have positive distances
def test_highsnobjects2()
	
	assert distance >= 0.


#make sure band sizes match so they can subtract correctly to calculate color
def test_highsnobjects3()

	assert len(j_band)==len(k_band)